interface:
  display_name: "GitCode Sync PR"
  short_description: "Sync commits across Ascend/pytorch branches"
  default_prompt: "Use $gitcode-sync-pr to port a commit from a source GitCode PR to one or more maintenance branches."
