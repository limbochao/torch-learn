interface:
  display_name: "GitCode Comment PR"
  short_description: "Post idempotent comments on GitCode PRs"
  default_prompt: "Use $gitcode-comment-pr to post the compile trigger comment on a GitCode pull request."
